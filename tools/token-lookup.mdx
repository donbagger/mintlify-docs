---
title: "Token Lookup Results"
sidebarTitle: "Token lookup"
description: "Search results for tokens matching your query"
---

### Token Search Results

<div id="dp-token-lookup"></div>

<div id="dp-token-results" style="display: none;">
  <div class="dp-results-header">
    <h3>Available Tokens</h3>
    <p id="dp-search-query"></p>
  </div>
  
  <div class="dp-table-container">
    <table class="dp-tokens-table">
      <thead>
        <tr>
          <th>Symbol</th>
          <th>Name</th>
          <th>Chain</th>
          <th>Current Price</th>
          <th>24h Volume</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody id="dp-results-table-body">
        {/* Results will be populated here */}
      </tbody>
    </table>
  </div>
  
  <div id="dp-no-results" style="display: none;">
    <div class="dp-no-results">
      <h4>No tokens found</h4>
      <p>Try searching for a different term or check the spelling.</p>
    </div>
  </div>
</div>

<style>
.dp-results-header {
  margin-bottom: 20px;
}

.dp-results-header h3 {
  margin-bottom: 8px;
  color: var(--color-text-primary);
}

.dp-results-header p {
  color: var(--color-text-secondary);
  font-size: 14px;
}

.dp-table-container {
  overflow-x: auto;
  border-radius: 8px;
  border: 1px solid var(--color-border);
}

.dp-tokens-table {
  width: 100%;
  border-collapse: collapse;
  background: var(--color-background);
}

.dp-tokens-table th {
  background: var(--color-background-secondary);
  padding: 12px 16px;
  text-align: left;
  font-weight: 600;
  color: var(--color-text-primary);
  border-bottom: 1px solid var(--color-border);
}

.dp-tokens-table td {
  padding: 12px 16px;
  border-bottom: 1px solid var(--color-border);
  color: var(--color-text-primary);
}

.dp-tokens-table tr:hover {
  background: var(--color-background-secondary);
}

.dp-token-symbol {
  font-weight: 600;
  color: var(--color-primary);
}

.dp-token-chain {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 500;
  text-transform: capitalize;
}

.dp-token-chain.ethereum {
  background: #627eea;
  color: white;
}

.dp-token-chain.solana {
  background: #9945ff;
  color: white;
}

.dp-token-price {
  font-weight: 600;
  color: var(--color-text-primary);
}

.dp-token-volume {
  color: var(--color-text-secondary);
  font-size: 14px;
}

.dp-token-actions {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.dp-action-btn {
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  text-decoration: none;
  color: var(--color-primary);
  background: var(--color-background-secondary);
  border: 1px solid var(--color-border);
  transition: all 0.2s;
}

.dp-action-btn:hover {
  background: var(--color-primary);
  color: white;
  text-decoration: none;
}

.dp-no-results {
  text-align: center;
  padding: 40px 20px;
  color: var(--color-text-secondary);
}

.dp-no-results h4 {
  margin-bottom: 8px;
  color: var(--color-text-primary);
}

.dp-loading {
  text-align: center;
  padding: 40px 20px;
  color: var(--color-text-secondary);
}

.dp-loading::after {
  content: '';
  display: inline-block;
  width: 20px;
  height: 20px;
  border: 2px solid var(--color-border);
  border-radius: 50%;
  border-top-color: var(--color-primary);
  animation: spin 1s ease-in-out infinite;
  margin-left: 8px;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}
</style>

> Search for tokens to see real-time data and available actions. Results include current prices, trading volume, and quick links to API endpoints.

<Note>
  **Pro tip:** This tool also works in Mintlify's global search bar! Try typing "SOL" or "USDC" in the search bar at the top of any page.
</Note>
